=link_to group_messages_path(group.id) do
  %li.group__list__name
    %h2
      =group.name
    %p
      - if group.messages[0]
        ="#{group.messages[0].body}"
      - else
        まだメッセージはありません
